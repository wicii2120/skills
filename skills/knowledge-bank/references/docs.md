# Documentation Branch

## Keywords

Derive keywords from tool and project names, commands, packages and modules, domains and stacks, artifacts such as `llms.txt` or `docs/`, and exact phrases in the request.

## Application

Apply `source:` and `fetch:` when the tool or project matches. Respect `limits:`.

## No match

Report exactly: `no docs knowledge recorded for this globally or locally`
