# Takeaway Branch

## Keywords

Derive keywords from the exact error or symptom, domain and stack names, packages, internal modules, commands, scripts, artifacts such as `README.md` or `Dockerfile`, and pattern terms such as `migration`, `router`, `cache`, or `screenshot diff`.

## Application

Apply `takeaway:` and `action:` only when `situation:` matches the current problem and its boundaries. Respect `limits:` and use `evidence:` to confirm that the mechanism and lesson fit.

## No match

Report exactly: `no takeaway recorded for this globally or locally`
